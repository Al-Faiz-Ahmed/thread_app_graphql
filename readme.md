# thread_app

Minimal GraphQL Threads API built with Node.js, Express, and TypeScript.

## Tech Stack

- Node.js (ES modules)
- TypeScript
- Express
- GraphQL (`graphql-http`, `@graphql-tools/schema`)
- Prisma (database + migrations)

## Getting Started

### 1. Install dependencies

```bash
npm install